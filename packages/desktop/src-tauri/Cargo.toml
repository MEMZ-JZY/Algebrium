[package]
name = "algebrium-desktop"
version = "0.1.0"
description = "Algebrium desktop application"
authors = ["Algebrium contributors"]
edition = "2021"

[lib]
name = "algebrium_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
